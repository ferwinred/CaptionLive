// Runtime configuration for the web apps. When served by the CaptionLive server this stays
// empty (same-origin API). The GitHub Pages build replaces it with {static: true, api: ""}.
window.CAPTIONLIVE = window.CAPTIONLIVE || {};
